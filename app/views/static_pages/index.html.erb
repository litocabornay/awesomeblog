<% provide(:title,"index") %>
<h1>ユーザー 一覧　(バンク)</h1>

<ul class="users">

<% @users.each do |user| %>
<% if current_user.admin? && !current_user?(user) %>
<%= user.name %>
<%= user.money %>
<%= link_to "入金", edit_static_page_path(user) %>

<br>
<% end %>
<% end %>

  
</ul>
